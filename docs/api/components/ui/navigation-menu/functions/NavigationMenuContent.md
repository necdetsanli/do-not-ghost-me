[**do-not-ghost-me**](../../../../README.md)

***

# Function: NavigationMenuContent()

> **NavigationMenuContent**(`props`): `Element`

Defined in: [src/components/ui/navigation-menu.tsx:189](https://github.com/necdetsanli/do-not-ghost-me/blob/2cf27d71497adc408791f4c93d855ac9fd7a3c78/src/components/ui/navigation-menu.tsx#L189)

Content panel for a navigation menu item.

- When `viewport` is enabled on the root, panels animate inside
  a shared viewport.
- When `viewport` is disabled, this behaves more like a popover
  attached directly under the trigger.

## Parameters

### props

`NavigationMenuContentProps` & `RefAttributes`\<`HTMLDivElement`\>

Content props including children and optional `className`.

## Returns

`Element`

The styled navigation menu content element.
