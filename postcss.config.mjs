// postcss.config.mjs
/** @type {import('postcss-load-config').Config} */
const config = {
  // PostCSS plugin chain for the app.
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
